services:
  opml:
    build:
      context: .
      target: production
    ports:
      - "8080:80"
    volumes:
      - ./src:/app/src:ro
      - ./static:/app/static:ro
      - ./files:/audiobooks:ro
      - ./data:/data
      - ./nginx.conf.template:/app/nginx.conf.template:ro
      - ./entrypoint.sh:/app/entrypoint.sh:ro
    environment:
      - FILES=/audiobooks
      - DATA=/data
      - PORT=3000
      - DEV_MODE=true
      # Uncomment to test /resync endpoint:
      # - ADMIN_USER=admin
      # - ADMIN_TOKEN=secret
      # - RECONCILE_INTERVAL=1800
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1/feed.opml"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
